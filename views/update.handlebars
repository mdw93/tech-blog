<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Post:</h3>

    <form class="form new-project-form">

   <div class="form-group myID" id="{{blogs.id}}">
    <label>Update your post</label>
    <input class="form-control" type="text" id="blog-name" name="blog-name" value="{{blogs.name}}">
  </div>

    <div class="form-group">
    <label>description:</label>
    <textarea class="form-control" id="description" rows="3">{{blogs.description}}</textarea>
  </div>

      <div class="form-group">
        <button type="submit" class="btn btn-primary" id="myUpdateButton">Update</button>
      </div>
    </form>
  </div>

 <script src="/js/update.js">  </script>